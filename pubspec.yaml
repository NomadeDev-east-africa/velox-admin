name: nomade_admin_clean
description: Panel admin Nomade 253 - Version propre
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  flutter_map: ^6.0.0
  latlong2: ^0.9.0
  cloud_firestore: ^5.4.4
  intl: ^0.19.0
  firebase_storage: ^12.3.4  # Upload images
  image_picker: ^1.1.1         # Choisir images
  file_picker: ^8.1.2          # Upload fichier menu (.txt)
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
